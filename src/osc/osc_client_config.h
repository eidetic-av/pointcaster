#pragma once

#include <fmt/format.h>
#include <map>
#include <string>
#include <tuple>

namespace pc::osc {

struct OscClientConfiguration {
  bool show_window = false;
  bool enable = true;
};

} // namespace pc::osc
